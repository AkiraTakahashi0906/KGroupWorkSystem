<UserControl x:Class="KGroupWorkSystem.Views.WorkOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <Label Content="作業指示作成"/>
            <Label Content= "{Binding TimeText.Value}"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="Start" Width="100" Command="{Binding StartButton}" Margin="5"/>
                <Button Content="Time" Width="100" Command="{Binding TimeButton}" Margin="5"/>
                <Button Content="Time2" Width="100" Command="{Binding Time2Button}" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
